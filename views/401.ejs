    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Server Error</title>
        <style>
            body { font-family: sans-serif; text-align: center; padding: 50px; }
            h1 { color: #0a5981; }
        </style>
    </head>
    <body>
        <h1><%= statuscode %>Unauthorized Request</h1>
        <p>You do not have the proper permissions to view the file/folder.</p>
        <% if (locals.errors && process.env.NODE_ENV !== 'production') { %>
            
            <p><%= locals.errors.msg %></p>
            <hr>
            <pre><%= locals.errors.stack %></pre>
        <% } %>
    </body>
    </html>