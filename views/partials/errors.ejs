<% if (locals.errors) {%>
  <ul class="errors" <%= locals.errors.length > 0 ? '' : 'hidden' %> >
   <% errors.forEach(function(error) { %>
      <li><%= error.msg %></li>
    <% }); %>
  </ul>
<% } %>