const { body, validationResult } = require("express-validator");
const passport = require("passport");

function getFileExplorer(req, res) {
  
  if (req.isAuthenticated()) {
  }
}

module.exports = {
  getFileExplorer,
  
};